@AGENTS.md

## Claude Code specifics

- The rules above are shared with every AI tool. Edit them in `AGENTS.md`, not here.
- Claude Code loads skills and subagents from `.claude/skills/` and `.claude/agents/`. `.agents/` holds the same files for other tools - change both together.
- Rule 7 applies to Claude: never commit, and never add `Co-Authored-By: Claude` or "Generated with Claude Code" anywhere.
